name: README build
on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Generate ASCII art from profile image
        shell: bash
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install pillow
          python3 ./scripts/image_to_ascii.py --input ./profile.jpg --output ./ascii-art.txt --width 40 --height 25
      - name: Generate SVG templates
        shell: pwsh
        run: ./New-ProfileSvg.ps1
      - name: Update README file
        env:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          USER_NAME: ${{ secrets.USER_NAME }}
        shell: pwsh
        run: ./today.ps1
      - name: Commit
        shell: pwsh
        run: |
          git add .
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          $changes = git diff-index --quiet HEAD 2>&1; $hasChanges = $LASTEXITCODE -ne 0
          if ($hasChanges) {
            git commit -m "Updated README"
            git push
          } else {
            Write-Host "No changes to commit"
          }
